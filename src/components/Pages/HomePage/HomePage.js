import styled from 'styled-components';

const Wapper666 = styled.p`
    font-size:2.5rem;
    color: olive;
`

const HomePage = (props) => {
    return ( 
        <Wapper666>
            Home----Page
        </Wapper666>
    );
}

export default HomePage;